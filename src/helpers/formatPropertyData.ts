import React from "react";
import { PropertyInformation } from "../types/api";

import HEADERS from "../data/translations.json";

export const formatPropertyData = (propertyData: unknown[]) => {
    
};
